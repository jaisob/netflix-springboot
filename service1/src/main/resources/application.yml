spring:
  application:
    name: Service1

server:
  port: 9001

eureka:
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
  client:
    service-url:
      defaultZone: http://127.0.0.1:9000/eureka